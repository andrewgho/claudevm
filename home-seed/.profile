# ~/.profile for claudevm VM
# Sourced by login shells (bash reads .bash_profile or .profile)

# Source .bashrc if it exists (for interactive login shells)
if [ -f "$HOME/.bashrc" ]; then
    . "$HOME/.bashrc"
fi
